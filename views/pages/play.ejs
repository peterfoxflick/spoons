<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <% include ../partials/nav.ejs %>

  <div class="container">
    <div class="row">
      <div class="col">
        <h1 class="display-2">Current Games</h1>
        <ul class="list-group" id="target-list">
        </ul>
      </div>
    </div>
    <div class="row">
      <div class="col">
        <h1 class="display-2">Join a Game</h1>
        <ul class="list-group" id="new-games-list">
        </ul>
      </div>
    </div>
    <div class="row">
      <div class="col">
        <h1 class="display-2">Past Games</h1>
      </div>
    </div>
  </div>

<script>
  function mainLoad(){
    $.get("/enroll/my?user_id=" + <%= user_id %>, function(data){
      var r = data.data;
      $("#target-list").empty();
      r.forEach(function(e){
        console.log(e);
        $("#target-list").append('<li class="list-group-item d-flex justify-content-between align-items-center">' + e.name + ' - ' + e.title + ' <span class="badge badge-primary badge-pill">Button Here</span></li>');
      })
    })

    $.get("/enroll/joined?user_id=" + <%= user_id %>, function(data){
      var r = data.data;
      $("#new-games-list").empty();
      r.forEach(function(e){
        console.log(e);
        $("#new-games-list").append('<li class="list-group-item d-flex justify-content-between align-items-center">' + e.title + ' - ' + e.name + ' <button type="button" onclick="leaveGame(' + e.id + ')" class="btn btn-outline-warning">Leave</button></li>');
      })


      $.get("/game/open?user_id=" + <%= user_id %>, function(data){
        var r = data.data;
        $("#new-games-list").empty();
        r.forEach(function(e){
          console.log(e);
          $("#new-games-list").append('<li class="list-group-item d-flex justify-content-between align-items-center">' + e.title + ' - ' + e.name + ' <button type="button" onclick="joinGame(' + e.id + ')" class="btn btn-outline-success">Join</button></li>');
        })
      })
    })

  }


function joinGame(id){
  $.post("/enroll/add?user_id=" + <%= user_id %> + "&game_id=" + id, function(data){
      mainLoad();
    })
  }

  mainLoad();


</script>

</body>
</html>
